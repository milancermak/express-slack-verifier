{
  "name": "express-slack-verifier",
  "version": "0.0.2",
  "description": "",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["express", "slack", "serverless"],
  "author": "Milan Cermak",
  "license": "MIT",
  "files": [
    "dist/**/*.ts",
    "dist/**/*.js"
  ],
  "devDependencies": {
    "typescript": ">=3.8.0"
  },
  "dependencies": {
    "@slack/events-api": "^2.3.4",
    "@types/express": "^4.16.0",
    "express": "^4.16.0"
  }
}
